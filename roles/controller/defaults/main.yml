---
awx_organization: Default
aws:
  region: us-east-1
azure:
  region: eastus
  resource_group: ansible_test
credentials:
  ssh:
    key_data: "{% raw %}{{ lookup('file', '~/.ssh/id_ed25519') }}{% endraw %}"
execution_environments:
  cloud_ee:
    set_default: true
job_templates:
  demo:
    delete_demo_template: false
  aws:
    vnet:
      subnet_name: subnet-test
      subnet_cidr: 10.0.200.0/24
  azure:
    log_analytics:
      workspace: ansible
    vnet:
      name: Content Lab - Azure - Create VNET
      route_table_name: vnet-rt
      vnet_name: vnet-test
      vnet_cidr: 10.0.200.0/23
      subnet_name: subnet-test
      subnet_cidr: 10.0.200.0/24
projects:
  skip_wait: false
seed_lab_content: false
seed_validated_content: true
